# LinoN 后端依赖 —— 钉死版本(plan §2 约束4:venv + 锁版本)。
# 兼容 Python 3.9+(开发机为 3.9.6;ECS Python 版本待定,这些版本 3.9~3.12 通吃)。
# 升级前先在 ECS 上 setup.sh 重建 venv 验证,勿在生产直接 pip install -U。

# —— 配置:pydantic-settings 读 .env(plan 锁定)——
pydantic==2.10.6
pydantic-settings==2.7.1

# —— 历史/资金/日历:Tushare 2000 积分会员(plan 锁定)——
tushare==1.4.21

# tushare 运行依赖(pandas 处理 DataFrame;2.2.3 是支持 Py3.9 的末版)
pandas==2.2.3
requests==2.32.3

# —— 阶段 1 FastAPI 脊椎 ——
fastapi==0.115.6
uvicorn==0.34.0

# —— 阶段 1 APNs 直连(token-based JWT ES256 + HTTP/2 推送)——
# PyJWT 做 ES256 JWT 签名;cryptography 提供 EC 后端;httpx[http2] 走 APNs HTTP/2 网关。
# 版本兼容 Python 3.9(开发机 3.9.6;ECS 3.9~3.12 通吃)。
PyJWT==2.9.0
cryptography==44.0.0
httpx[http2]==0.27.2

# —— 测试(TestClient 依赖 httpx,已在上方钉版本)——
pytest==8.3.4

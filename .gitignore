# —— 密钥(绝不提交)——
.env
*.env
!.env.example
backend/.env
# APNs 私钥(.p8)绝不入库——搬 ECS secret mode 600
*.p8
# 客户端本地密钥(API_TOKEN 经 LocalSecrets.plist 打进 bundle 时,绝不入库)
LocalSecrets.plist
client/**/LocalSecrets.plist

# —— SQLite 落盘 ——
*.db
*.db-journal
*.db-wal
*.db-shm
backend/data/*.db
data/*.db

# —— Python ——
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# —— venv ——
.venv/
venv/
env/
backend/.venv/
backend/venv/

# —— macOS ——
.DS_Store

# —— Xcode / SwiftPM 产物 ——
*.xcuserstate
xcuserdata/
DerivedData/
build/
*.xcworkspace/xcuserdata/
.swiftpm/
Package.resolved
